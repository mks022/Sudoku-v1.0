services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
  web:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api
